# Required: Your OpenAI API key (get one at https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-key-here

# Optional: Model to use (default: gpt-4.1)
OPENAI_MODEL=gpt-4.1

# Optional: Secret for signing session cookies (generate a random string)
COOKIE_SECRET=change-me-to-a-random-secret
